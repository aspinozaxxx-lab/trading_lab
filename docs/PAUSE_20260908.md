# Пауза исследования — 2026-09-08

Пользователь: «останови пока работу, сохрани все что надо».
Это точка продолжения, а не новый эксперимент или достижение цели доходности.
Новые гипотезы, расчёты, обучение, source sample и запуск paper не выполнять до
явного нового указания пользователя продолжить. Старые очереди и автоматическое
продолжение цели не отменяют эту паузу.

## Что сохранено

- Репозиторий: `D:\Projects\trading_lab`, ветка `agent/futoi-intraday-source`.
  Remote: <https://github.com/aspinozaxxx-lab/trading_lab>.
- Последний исследовательский commit до паузы:
  `2dd2d813c3887472bf293a6820d9bd1b278bd7c1` — результат V67; уже pushed.
  На начало сохранения рабочее дерево чистое и синхронизировано с origin.
- Код, протоколы, seals, tests и отчёты находятся в Git. Большие данные, модели и
  run-артефакты остаются вне Git: локально `D:\Projects\trading_lab_data`,
  на `gpu-mlserver` — `/srv/trading_lab_data`. Эти хранилища не объявляются полными
  зеркалами друг друга. Канонические результаты последних запусков лежат на сервере.
- Серверный код: `/opt/trading_lab`; Python: `/opt/trading_lab/.venv/bin/python`.
  При паузе вычислительный код, модели, sealed bytes и канонические результаты
  не менялись; повторные расчёты и копирование больших данных не выполнялись.

## Последний результат и канонические запуски

V65/V66/V67 вместе: **9 гипотез отсеяны на уровне 1, кандидатов уровня 2 — 0**.
Это итог именно трёх последних проверок, не число всех прежних экспериментов.
Проверенного достижения целевой доходности и разрешения на реальные сделки нет.

- [V65: четыре календарные гипотезы](V65_FAST_CONTEST_RESULT.md): все отклонены.
  `/srv/trading_lab_data/runs/v65_fast_calendar_contest_v1_daba08332161`.
- [V66 R1: четыре объёмные гипотезы](V66_FAST_VOLUME_RESULT.md): все отклонены;
  48 завершённых сценариев, R1 исправил только отчётный счётчик.
  `/srv/trading_lab_data/runs/v66_fast_volume_contest_v1_9cf51b328e26`.
  Исходная неудачная попытка `..._307db9d6583e` сохранена, не перезаписывать.
- [V67: покупка ОФЗ после аукциона](V67_OFZ_AUCTION_RESULT.md): REJECT_STAGE1,
  232 полных эпизода / 157 публикационных дней. Средний пятисессионный clean-price
  результат −0,0588% до затрат; −0,2588% / −0,4588% при обычных / удвоенных затратах.
  Это event study, не доходность портфеля: CAGR/Sharpe/MDD остаются null.
  `/srv/trading_lab_data/runs/v67_ofz_auction_concession_v1_bd3c7ee1c1d2`.
  Metrics SHA256: `3cda93a02c69f3b95dc431292065075f12d8fd48360308329ac8ae17ee70ba0f`.

Полные provenance, результаты проверок и ограничения — в этих отчётах и
[реестре экспериментов](EXPERIMENTS.md). Готовые тесты/аудиты не повторялись ради паузы.

## Что остановлено и что осталось работать

Операционная проверка `gpu-mlserver` — 2026-09-08 около 11:03 UTC / 14:03 MSK.
Известные исследовательские расчёты завершены; среди проверенных `trading-lab*`
units нет активного исследовательского service, среди процессов UID999 нет Python.
Не относящиеся к расчётам процессы не останавливались. Параллельные агенты не работают.

Выполнено `systemctl disable --now trading-lab-paper-bootstrap-20260909.timer`.
Последующая фактическая проверка:

- Timer: `disabled`, `inactive/dead`, `NextElapseUSecRealtime` пуст,
  `LastTriggerUSec` пуст. Запланированный запуск 9 сентября 00:01 MSK отменён.
- `trading-lab-paper-bootstrap-20260909.service`: `inactive/dead`, MainPID=0,
  ControlPID=0, время первого запуска пусто. Bootstrap не запускался.
- Paper instance
  `trading-lab-algopack-paper@f51c02902825f61ba74cfbbcdd719d7233bc1839f1a152d9289e35e9ea3624e0.service`:
  `not-found`, `inactive/dead`, MainPID=0. Paper не запускали и не инициализировали.
  Отдельный `test ! -e` точного account root завершился exit0: root отсутствует.
- Файлы operational units и подготовленный sealed bundle сохранены. Отключение
  удалило только ссылку автозапуска timer; данные, модели и журналы не удалялись.
- **Серверные сборщики исходных данных оставлены включёнными**, чтобы не терять
  новые наблюдения. При проверке 16 collector timers были `active/waiting`, включая
  witnessed AlgoPack FO. Это сохранение источников, не продолжение поиска стратегий
  и не сделки. Их расписания/код не менялись, данные/PnL для паузы не читались.
- Локальные Windows `TradingLab*` tasks не включались, новые PowerShell helpers
  не запускались. Старые failed units не сбрасывались и не перезапускались.

## Что делать после явного «продолжай»

1. Прочитать [STATUS](STATUS.md), [EXPERIMENTS](EXPERIMENTS.md) и
   [воронку проверок](HYPOTHESIS_FUNNEL.md). Приоритет пользователя: быстрая генерация
   и отсев гипотез → устойчивость → исполнение → демо. Не начинать с обслуживания
   таймеров, повторного обучения или повторения уже завершённых аудитов.
2. Не подбирать новые знаки/окна/контроли для отклонённых V65/V66/V67 под их результаты.
   Следующая экономическая гипотеза ещё не выбрана и не запущена. Защита данных 2026,
   причинность, комиссии и неизменность прежних канонических артефактов сохраняются.
3. [Индексный source sample](MOEX_INDEX_NEWS_SAMPLE_V1.md) остаётся отложенным:
   commit `5d39d27` сохранён, server deployment/collection не выполнены.
   Не запускать его автоматически вместо нового экономического теста.
4. Если пользователь возобновит именно AlgoPack paper, сначала проверить актуальное
   состояние и [правила запуска](ALGOPACK_PAPER_SERVICE.md), а не включать старый timer
   вслепую. Старая activation SHA:
   `f51c02902825f61ba74cfbbcdd719d7233bc1839f1a152d9289e35e9ea3624e0`,
   F=`2026-09-08T21:00:00UTC`. Пропущенные prospective окна не заполнять задним числом;
   sealed bytes и F не редактировать. При существующем/частичном account root никогда
   не повторять `--initialize`. Нужна отдельная проверка допустимого возобновления.
5. Обученные AlgoPack модели сохранены в
   `/srv/trading_lab_data/runs/algopack-paper-training/algopack_paper_training_v1_bb95784a169f`.
   Повторное обучение не требуется для восстановления контекста. Секреты не читать,
   не выводить и не копировать из `/etc/trading-lab/collector.env`.
